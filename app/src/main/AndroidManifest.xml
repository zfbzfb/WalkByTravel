<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walkbytravel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/tubuqu_leader_xiaomi"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".utils.ui.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".utils.ui.MainActivity" />
        <activity android:name=".utils.ui.WebViewActivity"></activity>
    </application>

</manifest>